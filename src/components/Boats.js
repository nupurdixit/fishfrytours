import React from 'react';
import './Boats.css';
export default function Boats(props) {
  return (
    <div className="task-card" draggable="true" id={[this.props.id]} onDragStart={this.props.onDragStart}>
      {this.props.name}
    </div>
  ) 
};